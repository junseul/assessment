# 지원자 평가와 LLM 시뮬레이션

## 지원자 선별에 활용하는 방법

성향파악·전략게임은 직무 관련 행동을 확인하는 보조 자료로 사용한다.
이 프로젝트의 점수만으로 합격자를 자동 결정하거나 사람의 성격·정직성을 확정하지 않는다.

1. **직무 기준을 먼저 정한다.** 채용 담당자와 현업이 실제 업무에서 필요한 행동을 정의한다.
   예: 품질관리에서는 오류 탐지와 확인 습관, 업무 조율에서는 약속 이행과 변화 대응을 확인한다.
   외향성 등 모든 성향이 높을수록 좋은 지원자라고 가정하지 않는다.
2. **응답의 해석 가능성을 확인한다.** 170문항의 응답 수, 미응답, 빠른 응답 비율,
   같은 답의 연속 반복, 극단 응답 비율을 함께 본다. 빠른 응답·반복 응답 하나만으로
   불성실·거짓말로 판정하지 않는다. 읽기 속도, 기기, 언어 이해도, 응시 조건을 재확인한다.
3. **자기보고와 행동을 교차 확인한다.** 성실성·학습민첩성 등을 높게 답했더라도
   게임의 오류·전략 수정 결과와 다르면 구조화 면접에서 구체적인 실제 경험을 묻는다.
   불일치는 질문의 출발점이지 탈락 근거가 아니다.
4. **직무 과제와 구조화 면접을 결합한다.** 동일 직무 지원자에게 같은 핵심 질문과
   평가 기준을 적용하고, 실제 업무와 가까운 과제 수행으로 확인한다. 최종 판단은 사람이 한다.
5. **실제 성과로 검증한다.** 사전에 정한 입사 후 직무성과와 검사 결과의 관계,
   재검사 안정성, 기존 면접·직무과제에 더해 얻는 정보, 응시 조건과 집단에 따른
   오류 차이를 검토한다. 개발에 쓰지 않은 표본에서 재확인한 뒤 가중치·합격선을 정한다.

근거: [OPM 평가 전략](https://www.opm.gov/policy-data-oversight/assessment-and-selection/assessment-strategy/),
[구조화 면접](https://www.opm.gov/policy-data-oversight/assessment-and-selection/other-assessment-methods/structured-interviews/),
[직무 과제·시뮬레이션](https://www.opm.gov/policy-data-oversight/assessment-and-selection/other-assessment-methods/work-samples-and-simulations/).

| 현재 기능 | 함께 확인할 지표 | 해석의 한계 |
| --- | --- | --- |
| 성향파악 | 10개 영역, 응답 수·품질, 면접의 행동 사례 | 자기보고이며 실제 행동의 증거와 구분 |
| 레이더 관제 | 누락·오경보, 정확도, 반응시간 변동 | 기기·집중 상태 영향 |
| 긴급 제동 | GO 누락, 조기 반응, SSD, SSRT | 적응형 과제의 정지 성공률만 비교하면 왜곡 가능 |
| 물류 분류센터 | 전환 비용, 전환·반복 오류, 고집 오류 | 속도와 정확도를 함께 해석 |
| 우주기지 일정관리 | 기본 과제 정확도, 사건·시간 약속 수행 | 이중 과제 특성, 응시 조건 확인 |
| 드론 추적 | 표적 수별 정확도·추적 용량 | 게임 숙련·시각 조건 영향 |
| 품질검사관 | 누락·오경보, 항목 수에 따른 탐색시간 | 실제 직무 과제와의 연결 검증 필요 |
| 암호 연구소 | 난도별 정확도·풀이시간 | 제한된 규칙 유형의 추론 과제 |
| 순간 통신 | T1 정확도, T1 정답일 때 T2 정확도, 신호 간격 | 표시 장치·프레임 타이밍 영향 |
| 탐사대 투자 | 선택 이력, 손실 후 행동, 반전 전후 변화 | 최종 자원은 운의 영향이 있어 단독 순위화 금지 |

현재 성향 점수는 평균 응답을 0~100으로 변환한 값이다. **백분위가 아니다.**
리포트의 50·75 기준과 게임별 해석 문구를 검증된 채용 합격선으로 간주하지 않는다.
이번 변경은 실제 지원자 채점·리포트 기준을 바꾸지 않는다.

## 실행 방법

개발 환경에서 관리자 로그인 → **로컬 테스트 → AI 시뮬레이션**.

- 기본 연결: `opencode-go/deepseek-v4-flash`. 기존 OpenCode Go API 인증을 사용한다.
- `OPENCODE_API_KEY`가 있으면 우선 사용하고, 없으면 현재 OS 사용자의
  `$XDG_DATA_HOME/opencode/auth.json` 또는 `~/.local/share/opencode/auth.json`에서
  `opencode-go`의 API 인증을 읽는다. 키를 화면·로그·결과에 출력하지 않는다.
- DeepSeek 직접 API는 `DEEPSEEK_API_KEY` 또는 OpenCode `deepseek` API 인증을 사용한다.
- 모델·가상 응시자·검사·실행 범위·시드를 선택한다. 추가 특성에는 실제 개인정보를 넣지 않는다.
- 빠른 테스트는 항목당 5회이며 채점·연결 확인용이다. 전체 성향검사는 170회,
  전체 9개 게임은 488회, 둘을 모두 실행하면 총 **658회 LLM 호출**이다.
- 실행 중 한 번에 한 건만 요청한다. 중지는 진행 중인 응답을 받은 뒤 적용된다.
- 오류 시 이전 결과를 유지하며 같은 단계에서 재시도할 수 있다. 자동 재시도는 하지 않는다.
- 응답 대기는 일반 항목 60초, 좌표가 많은 드론 항목 120초로 제한한다.
  제공자 오류·사용량 제한·응답 JSON 오류는 화면에 알리고 중단한다. 성공으로 대체하지 않는다.
- JSON 다운로드는 모델, 가상 특성, 시드, 문항·상황, 선택·설명, 결과, 완료 여부를 포함한다.
  브라우저 새로고침 전에 다운로드한다. 서버에 결과를 영구 저장하지 않는다.

OpenCode Go의 공식 API를 직접 호출하므로 CLI 설치·쉘 실행이 필요 없다.
공식 문서: [OpenCode Go API](https://opencode.ai/docs/go/#endpoints),
[DeepSeek JSON 응답](https://api-docs.deepseek.com/guides/json_mode/).

## 무엇을 검증하는가

LLM은 가상 인물의 행동 특성, 현재 문항·상황과 최근 10회 관찰 이력을 받아 실제로 답을 생성한다.
고정 점수나 임의의 정답을 만들어 응답 대신 사용하지 않는다. 성향 역채점·영역 점수·응답 품질은
기존 함수를 재사용한다. 탐사대 투자의 보상표·시행 수·반전 시점은 기존 서버 상수를 사용하되
모델에 전달하지 않는다. 모델에는 선택 결과의 자원 증감만 피드백한다.

같은 상황 시드로 신중형·서두름형·좋은 인상 응답형을 각각 실행해 응답 품질과 선택 이력을
비교할 수 있다. 결과가 설계 의도와 다르면 문항·해석 기준을 검토하는 단서로 삼는다.
LLM의 역할 연기는 실제 사람 집단이나 성격 유형을 대표하지 않으며, 그 차이로 채용 타당도를 증명할 수 없다.

## 실제 게임과의 차이

이 도구는 **텍스트 상황 시뮬레이션**이다. 실제 화면을 클릭하는 E2E 자동화가 아니다.
LLM이 숫자 좌표·전체 신호 순서·가상 시계를 읽을 수 있어 사람이 실시간으로 지각하는 조건과 다르다.
드론은 0.5초 간격 좌표 프레임, 순간 통신·긴급 제동은 시간 순서의 자극 설명을 사용한다.
우주기지는 1,900ms 단위 가상 시계로 127회 진행한다. 시각 렌더링·키보드 입력·네트워크 저장 흐름은 검증하지 않는다.

- `simulated_rt_ms`: LLM이 생성한 가상 반응시간. 마감 이후 행동은 무응답으로 처리한다.
- `llm_latency_ms`: 실제 API 호출에 걸린 시간. 사람의 인지능력 점수로 사용하지 않는다.
- 드론·암호 등의 난도 조절은 텍스트 시나리오에 적용된다. 사람 게임 엔진과 동일성은 보장하지 않는다.
- 텍스트 정확도와 부분 요약만 제공하며 사람의 SSRT·주의 깜빡임 지표를 산출했다고 주장하지 않는다.
- 일부 문항만 실행한 성향 영역 점수에는 응답 수를 표시한다. 표본이 작은 부분 점수를 비교·선발에 사용하지 않는다.
- 상황 시드는 재현 가능하지만 LLM 응답·호출시간은 동일하지 않을 수 있다.

## 변경 영향과 검증

- DB Schema/Migration: 없음. Candidate, SurveyResponse, GameResult 생성·수정 없음.
- 기존 API: 변경 없음. 기존 `/local-test/<stage>/`에 `simulation` stage를 추가했다.
  관리자 + DEBUG에서만 GET(화면), POST(시작/다음 단계)를 처리하며 CSRF를 유지한다.
- 실행 상태: 사용자 ID에 연결된 서명 토큰. 유효기간 24시간. 각 탭이 독립적으로 실행한다.
  같은 단계 재전송은 LLM을 다시 호출할 수 있으므로 중복 요청을 피한다.
- 배포: 수행하지 않는다. 운영에서는 DEBUG를 켜지 않는다. 개발 서버의 사용자만 인증 파일에 접근하면 된다.
- 검증 명령: `python manage.py test invites.test_simulation invites.tests traits.tests games.tests --settings=config.test_settings --noinput`
- 점검 명령: `python manage.py check --settings=config.test_settings`
- 롤백: 시뮬레이션 메뉴·stage 분기와 이번 신규 파일만 되돌린다. DB 롤백은 없다.

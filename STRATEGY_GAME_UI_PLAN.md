# STRATEGY_GAME_UI_PLAN

전략게임(탐사대 투자, `expedition-investment`)의 디자인·사용성 개선 계획.
화면 디자인(UI/UX)에 한정하며, 측정 방식·점수 산식·게임 규칙·난이도·DB·API는 변경하지 않는다.

## 1. 관련 파일

- `games/templates/games/expedition_investment.html` — 수정 대상 (유일)
- 그 외 전략게임 화면은 수정하지 않는다.

## 2. 현재 문제 (진단, 심각도순)

### P1 (판단·정보 파악을 어렵게 하는 문제)

1. **게임 상태를 한눈에 보기 어렵다.** 목표/현재 자원이 화면 본문에 잠깐만 노출되고, 각 선택 화면의 "누적 자원: N" 텍스트가 작아 놓치기 쉽다.
2. **진입 단계가 중복된다.** 시작 카드(버튼)와 게임 내 스페이스바 안내가 이어져 같은 안내를 두 번 보게 된다.
3. **결과 피드백이 색상에만 의존한다.** +는 초록, -는 빨강만으로 표시되어 색각 차이를 쓰지 못하는 경우 구분하기 어렵다. 또 금액 크기가 강조되어 특정 지역이 "정답처럼" 보일 소지가 있다.
4. **레이아웃이 시행마다 움직인다.** 선택 버튼(높이 큼)과 결과 텍스트(짧음)가 같은 영역을 오가며 화면이 들썩인다.

### P2. 시각적 완성도

5. **결과 화면 정보가 부족하다.** "완료/결과가 저장되었습니다"만 있고 최종 모은 자원이 표시되지 않는다.
6. **시작 카드의 정보 배치가 납작하다.** 긴 단락 한 줄로 규칙을 설명한다.

## 3. 수정할 내용

- **지속 상태 영역(HUD)** 추가: `목표`와 `현재 자원`을 게임 내내 상단 고정 표시. (현재 자원은 JS로 갱신)
- **시작 카드 재구성** — 부가 텍스트(`STRATEGY GAME · 9/9`), 제목, 안내를 분리.
- **게임 내 스페이스바 안내 게이트 제거** — 시작 버튼이 준비 게이트 역할을 하므로 중복 제거.
- **선택 버튼 전용 스타일** (`exp-choice`) — 명확한 구분선·hover·focus-visible, 지역이 "정답처럼" 보이지 않는 단조로운 배치.
- **결과 피드백 재구성** — "획득/손실" 텍스트 + 부호 + 색상으로 비색상 단서 추가, 금액 크기 과장 완화.
- **진행 영역 min-height 확보** — 시행마다 레이아웃이 움직이지 않게 예약 공간.
- **결과 화면에 최종 자원 표시** (화면 표시만, 계산 로직 불변).
- 전용 반응형(작은 화면에서 상태 영역/버튼 줄바꿈) 및 접근성(aria-live, focus-visible).

## 4. 수정하지 않을 내용

- 점수 산식, 게임 규칙, 난이도, 측정 방식, 행동 로그 구조 — 불변
- `games/views.py`, `games/catalog.py`, `games/urls.py`, DB 모델·마이그레이션
- 다른 8개 검사게임 템플릿, 관리 그리드(`admin_grid.html`), 공용 `lightdash.css`, `game-progress.js`
- `/games/` 목록, 헤더/내비, 로그인/권한

## 5. 다른 검사게임에 미칠 수 있는 영향

- 없음. 모든 CSS·JS 변경은 `expedition_investment.html` 내부 `extra_head`/`extra_scripts`에 국한되어 다른 게임에 닿지 않는다.
- 공용 자산(`lightdash.css`, `game-progress.js`)은 손대지 않는다.

## 6. 검증 방법

1. Django 시스템 체크 (`manage.py check`)
2. `games` 테스트 실행 — 기존 실패(2건, 이 프로젝트 기존 문제)를 제외한 회귀 확인
3. 후보 면접 등 다른 게임 렌더링 200 및 화면 구조 불변 확인
4. 렌더링된 전략게임 HTML에서 변경된 상태 영역/버튼 클래스/결과 표시 확인
5. 시작→50시행→결과 흐름의 JS 문법·로직 회귀 확인 (점수 계산 불변)
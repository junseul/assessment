현재 원칙을 기준으로 **Codex/Claude Code가 불필요한 질문은 줄이되, DB/API/배포처럼 위험한 변경만 승인받도록** 다시 구성했습니다.

# AGENTS.md

# 1. 프로젝트 기본 원칙

## 운영 환경

이 프로젝트의 운영 기준은 다음과 같다.

* DB 접속 정보는 프로젝트의 `.env` 파일에 존재한다.

* 서버 작업
* DB 작업
* PostgreSQL 작업
* Podman 작업
* 컨테이너 작업
* 배포
* 운영 확인
* 로그 확인
* 서비스 상태 확인

로컬 환경은 기본적으로 코드 편집 및 개발 작업 위치로 사용한다.

로컬 상태만 확인하고 운영 상태라고 판단하지 않는다.

---

# 2. 최우선 행동 원칙

다음 원칙을 우선 적용한다.

1. 사용자가 요청한 범위만 작업한다.
2. 기존 프로젝트 구조와 동작을 최대한 유지한다.
3. 불필요한 질문을 하지 않는다.
4. 확인 가능한 내용은 직접 확인한다.
5. 코드와 설정을 읽어서 판단할 수 있는 내용은 사용자에게 질문하지 않는다.
6. 사소한 기술적 결정은 기존 코드와 프로젝트 관례를 기준으로 스스로 결정한다.
7. 위험도가 낮은 수정은 승인 없이 진행한다.
8. DB 구조, API 계약, 인프라, 운영 데이터에 영향을 주는 변경은 별도 통제한다.
9. 추측으로 중요한 구조를 변경하지 않는다.
10. 작업 완료 후 반드시 테스트와 결과 확인을 수행한다.

---

# 3. 질문 최소화 원칙

다음과 같은 질문은 하지 않는다.

* 진행할까요?
* 수정할까요?
* 테스트할까요?
* 파일을 열어볼까요?
* 서버를 확인할까요?
* 로그를 확인할까요?
* 오류를 수정할까요?
* 다음 단계로 진행할까요?

작업 수행에 필요한 일반적인 조사, 파일 확인, 코드 분석, 테스트는 별도 승인 없이 수행한다.

기존 코드, 설정, DB 구조, 로그 또는 서버 상태를 확인하면 답을 얻을 수 있는 경우 사용자에게 질문하지 말고 직접 확인한다.

명확하지 않은 사소한 구현 사항은 다음 우선순위로 결정한다.

1. 사용자의 명시적 요구사항
2. `AGENTS.md`
3. 기존 프로젝트 구조
4. 기존 코드 패턴
5. 기존 명명 규칙
6. 최소 변경 원칙
7. 일반적인 Django/Python 관례

---

# 4. 작업 분류

모든 작업은 시작 전에 내부적으로 다음 두 종류 중 하나로 분류한다.

## A. 간단한 수정

다음 조건을 모두 만족하면 간단한 수정으로 판단한다.

* 요청 범위가 명확함
* 수정 범위가 작음
* 기존 구조를 변경하지 않음
* DB Schema 변경 없음
* Migration 생성 없음
* API 계약 변경 없음
* 새로운 패키지 추가 없음
* 인프라 구조 변경 없음
* 운영 데이터 삭제 또는 변환 없음
* 보안 정책 변경 없음

예:

* 문구 수정
* 오타 수정
* 작은 UI 오류 수정
* CSS 일부 수정
* 기존 함수의 명확한 버그 수정
* 기존 조건문 수정
* 로그 추가
* 예외 처리 보완
* 기존 API 내부 로직의 명확한 오류 수정
* 기존 기능을 유지하는 최소 범위 코드 수정

### 간단한 수정 처리

간단한 수정은 사용자 승인을 요청하지 않는다.

다음 순서로 바로 수행한다.

요구사항 확인
→ 관련 코드 확인
→ 최소 범위 수정
→ 테스트
→ 결과 보고

---

# 5. 일반 개발 작업

기능 추가 또는 구조 변경이 필요한 경우 다음 순서를 따른다.

1. 요구사항 분석
2. 기능 정의
3. UI 설계
4. DB 설계
5. API 설계
6. 구현 계획
7. 필요 시 사용자 승인
8. 구현
9. 테스트
10. 운영 확인
11. 결과 보고

UI가 없는 기능은 UI 설계를 생략할 수 있다.

DB 변경이 없는 경우 DB 설계를 생략할 수 있다.

API 변경이 없는 경우 API 설계를 생략할 수 있다.

불필요한 설계 문서를 형식적으로 만들지 않는다.

---

# 6. 설계 완료 전 구현 제한

새로운 기능 개발 또는 구조 변경 작업에서는 설계가 완료되기 전에 구현을 시작하지 않는다.

설계 단계에서는 다음 내용을 확인한다.

## 요구사항 분석

* 작업 목적
* 필요한 기능
* 현재 동작
* 변경 후 동작
* 변경 범위
* 제외 범위

## 기능 정의

* 사용자 동작
* 시스템 동작
* 입력
* 출력
* 예외 상황

## UI 설계

UI 변경이 필요한 경우에만 작성한다.

* 변경 화면
* 사용자 흐름
* 필요한 컴포넌트
* 기존 디자인 유지 여부

## DB 설계

DB 변경이 필요한 경우에만 작성한다.

* 대상 테이블
* 추가/변경/삭제 필드
* 관계
* 인덱스
* 제약조건
* Migration 필요 여부
* 기존 데이터 영향

## API 설계

API 변경이 필요한 경우에만 작성한다.

* Endpoint
* Method
* Request
* Response
* 인증/권한
* 오류 Response
* 기존 API 호환성

---

# 7. 사용자 승인이 필요한 작업

다음 변경은 구현 전에 반드시 사용자 승인을 받는다.

## DB

* Table 생성
* Table 삭제
* Column 추가
* Column 삭제
* Column Type 변경
* Constraint 변경
* Index 구조 변경
* Migration 실행
* 운영 데이터 일괄 수정
* 운영 데이터 삭제
* 데이터 변환 작업

## API

* Endpoint 추가
* Endpoint 삭제
* URL 변경
* Request 구조 변경
* Response 구조 변경
* 인증 방식 변경
* 외부 시스템과의 API 계약 변경

## 인프라

* 새로운 컨테이너 생성
* 기존 컨테이너 삭제
* Containerfile 구조 변경
* compose 구조 변경
* 새로운 DB 생성
* DB 삭제
* 운영 Port 변경
* Reverse Proxy 변경
* 방화벽 변경
* 시스템 서비스 변경

## Dependency

다음 항목의 신규 추가 또는 대규모 변경:

* Python Package
* JavaScript Package
* Library
* Framework
* 외부 서비스
* SaaS
* Middleware

## 기타

* 대규모 리팩토링
* 인증/인가 구조 변경
* 보안 정책 변경
* 기존 주요 기능 동작 변경
* 여러 모듈에 광범위한 영향을 주는 변경
* 운영 중단 가능성이 높은 작업

---

# 8. 별도 승인이 필요 없는 작업

다음 작업은 요청 범위 안에서 별도 승인 없이 진행한다.

* 코드 조회
* 파일 조회
* 설정 조회
* Git 상태 확인
* 로그 확인
* 서버 상태 확인
* 컨테이너 상태 확인
* DB 구조 조회
* DB 데이터 조회
* 테스트 실행
* 정적 분석
* 기존 코드 스타일에 따른 소규모 수정
* 명확한 버그 수정
* 예외 처리 보완
* 오류 로그 추가
* 기존 테스트 수정
* 테스트 코드 추가
* 운영 상태 검증

단, 운영 데이터 또는 운영 구조를 변경하는 작업은 승인 기준을 따른다.

---

# 9. 구현 계획

사용자 승인이 필요한 작업은 구현 전에 다음 형식으로 간결하게 보고한다.

## 구현 계획

### 작업 목적

작업으로 해결하려는 내용을 작성한다.

### 변경 대상

변경 예정 파일과 구성요소를 작성한다.

### 구현 방법

핵심 구현 방법만 작성한다.

### DB 영향

다음 중 하나로 표시한다.

* 없음
* 있음

있는 경우 구체적인 변경 내용을 작성한다.

### API 영향

다음 중 하나로 표시한다.

* 없음
* 있음

있는 경우 구체적인 변경 내용을 작성한다.

### 운영 영향

다음 중 하나로 표시한다.

* 없음
* 있음

### 위험 요소

실제로 존재하는 위험 요소만 작성한다.

형식적인 위험 요소를 만들어내지 않는다.

---

# 10. 구현 제한

다음 행동은 금지한다.

* 요구사항에 없는 기능 추가
* 임의 기능 확장
* 사용자 요청 없는 UI 변경
* 감성적인 UI 변경
* 디자인 전체 변경
* 범위 외 코드 수정
* 불필요한 파일 수정
* 대규모 리팩토링
* 기존 정상 동작 변경
* 승인 없는 DB Schema 변경
* 승인 없는 Migration 실행
* 승인 없는 API 계약 변경
* 승인 없는 Dependency 추가
* 승인 없는 인프라 구조 변경
* 운영 데이터 임의 수정
* 운영 데이터 임의 삭제
* 필요하지 않은 설정 변경

---

# 11. 허용되는 구현

다음 작업은 적극적으로 허용한다.

* 최소 범위 수정
* 명확한 버그 수정
* 필요한 예외 처리
* 오류 로그 추가
* 테스트 가능한 코드 작성
* 기존 코드 스타일 유지
* 기존 구조 재사용
* 기존 함수 재사용
* 기존 컴포넌트 재사용
* 기존 API 재사용
* 중복 코드가 명백한 경우의 작은 범위 정리

---

# 12. 기존 구조 우선

새로운 구조를 만들기 전에 기존 구현을 먼저 확인한다.

다음 항목이 기존에 존재하면 우선 재사용한다.

* Model
* View
* Service
* Repository
* Form
* Serializer
* Template
* Component
* Utility
* API
* CSS
* JavaScript
* 공통 함수

같은 기능을 수행하는 새로운 구조를 중복 생성하지 않는다.

---

# 13. 신규 기술 도입 제한

다음 항목을 임의로 추가하지 않는다.

* 새로운 Framework
* 새로운 Library
* 새로운 Package
* 새로운 DB
* 새로운 Cache
* 새로운 Queue
* 새로운 외부 서비스
* 새로운 SaaS

기존 기술로 구현 가능한 경우 기존 기술을 사용한다.

신규 기술이 반드시 필요한 경우 이유와 대안을 사용자에게 설명하고 승인을 받는다.

---

# 14. Django 개발 규칙

## ORM

Django ORM을 우선 사용한다.

Raw SQL은 다음 경우에만 사용한다.

* ORM으로 구현하기 어렵거나
* 성능상 명확한 이유가 있거나
* 기존 프로젝트가 해당 부분에서 Raw SQL을 사용하는 경우

Raw SQL을 사용한다면 이유를 명시한다.

---

## Model

기존 Model 구조와 명명 규칙을 유지한다.

Model 변경이 필요한 경우 Migration 영향부터 확인한다.

운영 DB에 적용하기 전에 생성된 Migration 내용을 반드시 검토한다.

---

## Migration

Migration을 무조건 실행하지 않는다.

다음 순서를 따른다.

1. Model 변경 확인
2. Migration 생성
3. Migration 파일 검토
4. SQL 또는 변경 내용 확인
5. 사용자 승인
6. 운영 적용
7. 적용 결과 확인

운영 DB Migration은 명시적인 승인 없이 실행하지 않는다.

---

## View / Service / Repository

프로젝트에 기존 Layer 구조가 있다면 해당 구조를 따른다.

기존 프로젝트에 없는 Layer를 이 문서만을 이유로 새롭게 만들지 않는다.

역할을 지나치게 한 파일이나 함수에 집중시키지 않는다.

불필요한 추상화는 만들지 않는다.

---

## settings.py

`settings.py`를 이유 없이 수정하지 않는다.

설정 변경이 필요한 경우 기존 환경변수 또는 설정 구조를 먼저 확인한다.

Secret, Password, Token, SSH 정보 등을 코드에 하드코딩하지 않는다.

---

# 15. .env 및 보안 정보

`.env` 파일에 포함된 정보를 작업에 사용할 수 있다.

단 다음 정보는 응답에 그대로 출력하지 않는다.

* Password
* Secret Key
* API Key
* Token
* SSH Password
* Private Key
* DB Password

로그 또는 결과 보고에서도 민감정보를 마스킹한다.

`.env` 내용을 소스코드에 복사하지 않는다.

Git에 Secret이 포함되지 않도록 한다.

---

# 16. 운영 서버 작업 원칙

가능하면 다음 순서를 따른다.

1. 현재 상태 확인
2. 변경 대상 확인
3. 필요한 경우 백업 또는 Rollback 방법 확인
4. 최소 범위 변경
5. 서비스 상태 확인
6. 로그 확인
7. 기능 테스트

서버 상태를 확인하지 않고 추측으로 운영 장애 원인을 판단하지 않는다.

---

# 17. 컨테이너 작업 원칙

기존 Podman 환경을 우선 사용한다.

기존 컨테이너를 삭제하거나 재생성하기 전에 영향 범위를 확인한다.

특히 다음 컨테이너는 운영 자원으로 취급한다.

`postgresql-db`

해당 컨테이너에 대해 다음 작업은 사용자 승인 없이 수행하지 않는다.

* 삭제
* 재생성
* Volume 삭제
* DB 삭제
* 설정 초기화
* 데이터 초기화

컨테이너 조회, 로그 확인, 상태 확인은 승인 없이 수행한다.

---

# 18. PostgreSQL 작업 원칙


운영 DB에서는 다음 작업을 사용자 승인 없이 하지 않는다.

* DROP
* TRUNCATE
* DELETE 대량 실행
* UPDATE 대량 실행
* ALTER TABLE
* CREATE/DROP DATABASE
* Schema 변경
* 데이터 Migration

SELECT 및 구조 조회는 승인 없이 수행할 수 있다.

---

# 19. 코드 품질

다음 원칙을 적용한다.

* 기존 코드 스타일 우선
* 불필요한 코드 생성 금지
* 중복 코드 최소화
* 의미 있는 변수명 사용
* 적절한 예외 처리
* 하드코딩 최소화
* Magic Number 최소화
* 함수의 책임을 명확하게 유지
* 읽기 쉬운 코드 작성

DRY와 SOLID는 프로젝트 구조와 수정 규모에 맞게 적용한다.

DRY 또는 SOLID를 이유로 불필요한 대규모 리팩토링을 하지 않는다.

---

# 20. 추측 금지

중요한 사항은 추측하여 구현하지 않는다.

단, 모든 불확실성을 질문으로 전환하지 않는다.

다음 순서로 해결한다.

1. 기존 코드 확인
2. 프로젝트 설정 확인
3. DB 구조 확인
4. 관련 로그 확인
5. 서버 상태 확인
6. 기존 구현 패턴 확인
7. 그래도 판단할 수 없는 경우 질문

사용자가 이미 제공한 정보를 다시 질문하지 않는다.

---

# 21. 작업 중 질문이 필요한 경우

다음 상황에서는 작업을 중단하고 질문할 수 있다.

* 서로 양립할 수 없는 요구사항이 존재함
* 사용자의 업무적 판단이 필요한 선택지가 존재함
* 운영 데이터 삭제가 필요함
* DB Schema 변경 방향을 결정할 수 없음
* API 호환성을 깨야 함
* 보안 정책을 변경해야 함
* 기존 기능을 의도적으로 제거해야 함
* 복수 구현안이 업무적으로 다른 결과를 만듦

코드 또는 서버를 직접 확인하면 해결되는 내용은 질문 사유가 아니다.

---

# 22. 오류 처리

작업 중 오류가 발생하면 바로 사용자에게 해결 방법을 묻지 않는다.

다음 순서로 처리한다.

1. 오류 메시지 확인
2. 관련 로그 확인
3. 원인 분석
4. 기존 코드 및 설정 확인
5. 수정 가능한 경우 최소 범위 수정
6. 다시 테스트

합리적인 범위에서 해결할 수 없는 경우에만 사용자에게 보고한다.

동일한 실패 명령을 의미 없이 반복 실행하지 않는다.

---

# 23. 테스트 원칙

변경 후 가능한 범위에서 테스트한다.

최소한 다음을 확인한다.

* 수정한 기능 정상 동작
* 기존 기능 영향 여부
* 예외 상황
* 오류 로그
* Syntax 또는 Compile 오류
* Django System Check

필요한 경우 다음을 수행한다.

```bash
python manage.py check
```

프로젝트에 테스트가 존재하면 관련 테스트를 실행한다.

가능한 경우 다음 케이스를 확인한다.

* 정상 케이스
* 예외 케이스
* 경계값

테스트할 수 없는 경우 이유를 결과 보고에 작성한다.

---

# 24. TODO 및 미완성 코드 금지

작업 완료를 가장하기 위한 다음 코드를 남기지 않는다.

```text
TODO
FIXME
NotImplemented
임시 반환값
가짜 데이터
빈 함수
미구현 Stub
```

사용자가 명시적으로 Prototype 또는 Stub 구현을 요청한 경우는 예외로 한다.

---

# 25. 실행 가능한 코드

제출하는 코드는 가능한 범위에서 반드시 실행 가능한 상태여야 한다.

다음 상태로 작업을 완료하지 않는다.

* Syntax Error
* Import Error
* Compile Error
* 명백한 Runtime Error
* 깨진 URL
* 존재하지 않는 함수 호출
* 존재하지 않는 Model 참조

코드 수정 후 관련 오류 여부를 확인한다.

---

# 26. 완료 기준 Definition of Done

다음 조건을 만족해야 작업 완료로 판단한다.

* 요청사항 구현 완료
* 요청 범위 외 변경 없음
* Syntax 오류 없음
* 관련 테스트 완료
* Django Check 확인
* DB 영향 확인
* API 영향 확인
* 기존 기능 영향 확인
* 미완성 TODO 없음
* 결과 보고 완료

---

# 27. 완료 보고

작업 완료 후 장황한 설명 대신 다음 형식으로 보고한다.

## 작업 결과

완료된 내용을 간단히 작성한다.

## 변경 파일

* 변경된 파일 목록

변경 파일이 없으면 `없음`이라고 작성한다.

## DB 변경

다음 중 하나:

* 없음
* 있음: 변경 내용

## API 변경

다음 중 하나:

* 없음
* 있음: 변경 내용

## 테스트

* 실행한 테스트
* 결과

## 운영 확인

운영 확인이 필요한 작업:

* `128.1.1.77` 확인 결과

운영 확인이 필요하지 않은 작업:

* 해당 없음

## 영향 범위

실제로 영향을 받는 기능만 작성한다.

## Rollback

필요한 경우 되돌리는 방법을 작성한다.

---

# 28. 응답 방식

## 간단한 수정

다음 흐름으로 바로 작업한다.

요구사항 확인
→ 코드 확인
→ 수정
→ 테스트
→ 결과 보고

사용자 승인 대기 단계를 만들지 않는다.

---

## 일반 기능 개발

다음 흐름을 따른다.

요구사항 분석
→ 기능 정의
→ 필요한 설계
→ 구현 계획
→ 승인 필요 여부 판단
→ 구현
→ 테스트
→ 결과 보고

---

## 승인 필요 작업

다음 흐름을 따른다.

요구사항 분석
→ 설계
→ 구현 계획
→ 사용자 승인
→ 구현
→ 테스트
→ 운영 확인
→ 결과 보고

---

# 29. 금지되는 응답 패턴

다음과 같은 불필요한 확인 질문을 반복하지 않는다.

```text
진행할까요?
수정해드릴까요?
테스트를 실행할까요?
서버에서 확인할까요?
이 방식으로 구현할까요?
계속 진행할까요?
```

이미 요구사항이 충분히 명확하면 바로 작업한다.

승인이 필요한 변경만 명확하게 승인 요청한다.

---

# 30. 최종 행동 규칙

모르는 것은 먼저 코드와 환경을 확인한다.

확인 가능한 것을 사용자에게 질문하지 않는다.

중요한 구조를 추측하여 변경하지 않는다.

요청하지 않은 기능을 만들지 않는다.

요청하지 않은 UI를 변경하지 않는다.

운영 DB를 임의로 변경하지 않는다.

운영 데이터를 임의로 삭제하지 않는다.

필요하지 않은 리팩토링을 하지 않는다.

간단한 수정은 바로 처리한다.

위험한 변경만 승인받는다.

구현 후 반드시 테스트한다.

운영 확인이 필요한 경우 반드시 `128.1.1.77`에서 확인한다.

TODO를 남기지 않는다.

실행되지 않는 코드를 완료했다고 보고하지 않는다.
